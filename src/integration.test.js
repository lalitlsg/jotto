import { storeFactory } from "../test/testUtils";

describe("guessed words action dispatcher", () => {
  describe("no guessed words", () => {
    test("update state correctly for unsuccessful guess", () => {});
    test("update state correctly for successful guess", () => {});
  });
  describe("some guessed words", () => {
    test("update state correctly for unseccessful guess", () => {});
    test("update state correctly for seccessful guess", () => {});
  });
});
